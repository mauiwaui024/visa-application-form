version: "3.8"

services:
  postgres:
    image: postgres:15-alpine
    container_name: visa-application-db
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_INITDB_ARGS: "--encoding=UTF8 --lc-collate=C --lc-ctype=C"
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/001_init.sql:ro
    networks:
      - postgres-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

volumes:
  postgres_data:
    driver: local

networks:
  postgres-net:
    driver: bridge
# services:
#   postgres:
#     image: postgres:15-alpine
#     container_name: postgres-db-spectrum
#     environment:
#       POSTGRES_DB: spectrum-db-m
#       POSTGRES_USER: spectrum-dev
#       POSTGRES_PASSWORD: 112233aa
#     ports:
#       - "5420:5432"
#     volumes:
#       - postgres_data:/var/lib/postgresql/data
#       - ./init:/docker-entrypoint-initdb.d
#     networks:
#       - postgres-net

# volumes:
#   postgres_data:

# networks:
#   postgres-net: